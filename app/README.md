# poseperfect

The Flutter app shell. iOS is the acceptance target; Android is kept compiling
as a fast local test loop (map decision 15).

Built from Linux via GitHub Actions macOS runners - see
[`docs/ios-pipeline.md`](../docs/ios-pipeline.md).
